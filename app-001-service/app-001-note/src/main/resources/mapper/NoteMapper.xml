<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="icu.huajuan.mapper.NoteMapper">


    <select id="load" resultType="icu.huajuan.model.note.vo.NoteCartVo">
        select t1.id, t1.model_type, t1.title, t1.`desc`, t2.imageId, t2.image_url
        from note t1
                 left join (select min(id) as imageId, note_Id, image_url from image_gallery group by note_Id, image_url) t2
                           on t1.id = t2.note_id
    </select>
</mapper>