server:
  port: 51003

spring:
  application:
    name: app-001-file
  cloud:
    nacos:
      discovery:
        server-addr: 152.136.176.118:8848
        namespace: 3c45f634-bff8-4f92-8bef-d872bdd9e6d8
      config:
        namespace: 3c45f634-bff8-4f92-8bef-d872bdd9e6d8
        group: DEFAULT_GROUP
        file-extension: yml
        server-addr: 152.136.176.118:8848
    loadbalancer:
      cache:
        enabled: true

# 七牛云配置------------------------------------
qiniu:
  accessKey: "5OLBaHyRVo1lZa82hVPir4E7E0om7UoJaAcakWs-"
  secretKey: "zzJbnm0MvRA_EElJVo1k2OWz45pIojL6G6xSDWcZ"
  # 七牛云空间名
  bucket: "app-001-file"
  # 外链访问路径
  hosts: "http://files.huajuan.icu/"
  priKey: ""
  # 地域(华东 z0 华北 z1 华南 z2 北美 na0 东南亚 as0)
  region: "z1"

mybatis-plus:
  # Mapper 所对应的 XML 文件位置
  mapper-locations: classpath:mapper/**/*.xml
  global-config:
    db-config:
      # 逻辑删除已删除值
      logic-delete-value: 1
      # 逻辑删除未删除值
      logic-not-delete-value: 0
      # 全局的 entity 的逻辑删除字段属性名,(逻辑删除下有效)
      logic-delete-field: deleted
  configuration:
    # 是否开启自动驼峰命名规则
    map-underscore-to-camel-case: true
    # 关闭 Mybatis 二级缓存，默认为 true
    cache-enabled: false
    # XXXXXXX
    call-setters-on-nulls: true

